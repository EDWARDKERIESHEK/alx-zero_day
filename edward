#include <stdio.h>

int main() {
    // print lowercase alphabet
    for (char c = 'a'; c <= 'z'; c++) {
        putchar(c);
    }

    // print uppercase alphabet
    for (char c = 'A'; c <= 'Z'; c++) {
        putchar(c);
    }

    // print new line
    putchar('\n');

    return 0;
}
#include <stdio.h>

int main() {
    // print lowercase alphabet
    for (char c = 'a'; c <= 'z'; c++) {
        putchar(c);
    }

    // print uppercase alphabet
    for (char c = 'A'; c <= 'Z'; c++) {
        putchar(c);
    }

    // print new line
    putchar('\n');

    return 0;
}
#include <stdio.h>

int main() {
    // print lowercase alphabet
    for (char c = 'a'; c <= 'z'; c++) {
        putchar(c);
    }

    // print uppercase alphabet
    for (char c = 'A'; c <= 'Z'; c++) {
        putchar(c);
    }

    // print new line
    putchar('\n');

    return 0;
}
#include <stdio.h>

int main() {
    // print lowercase alphabet
    for (char c = 'a'; c <= 'z'; c++) {
        putchar(c);
    }

    // print uppercase alphabet
    for (char c = 'A'; c <= 'Z'; c++) {
        putchar(c);
    }

    // print new line
    putchar('\n');

    return 0;
}
#include <stdio.h>

int main() {
    // print lowercase alphabet
    for (char c = 'a'; c <= 'z'; c++) {
        putchar(c);
    }

    // print uppercase alphabet
    for (char c = 'A'; c <= 'Z'; c++) {
        putchar(c);
    }

    // print new line
    putchar('\n');

    return 0;
}
#include <stdio.h>
#include <stdio.h>

int main() {
    // print lowercase alphabet
    for (char c = 'a'; c <= 'z'; c++) {
        putchar(c);
    }

    // print uppercase alphabet
    for (char c = 'A'; c <= 'Z'; c++) {
        putchar(c);
    }

    // print new line
    putchar('\n');

    return 0;
}

int main() {
    // print lowercase alphabet
    for (char c = 'a'; c <= 'z'; c++) {
        putchar(c);
    }

    // print uppercase alphabet
    for (char c = 'A'; c <= 'Z'; c++) {
        putchar(c);
    }

    // print new line
    putchar('\n');

    return 0;
}

